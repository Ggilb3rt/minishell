#include "minishell.h"

/* prompt_loop : create the loop that will run minishell
 *
 * int status;
	char *line;

	status = 1;
	while (status) {
        if (!ms_strcmp(line, "exit"))
            break ;
		line = readline("user@root > ");
        add_history(line);
	}
 */

int main(void)
{
	//prompt loop
    return (0);
}